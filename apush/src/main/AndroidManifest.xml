<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="cn.itsite.apush">

    <application
        android:allowBackup="true"
        android:label="@string/app_name"
        android:supportsRtl="true">


        <!-- 以下阿里云推送 -->
        <meta-data
            android:name="com.alibaba.app.appkey"
            android:value="24632152" /> <!-- 请填写你自己的- appKey -->
        <meta-data
            android:name="com.alibaba.app.appsecret"
            android:value="077b8ea013652e988304c0bc6b41fb3a" /> <!-- 请填写你自己的appSecret -->
        <!-- 以上阿里云推送 -->

        <activity
            android:name="cn.itsite.apush.PopupPushActivity"
            android:exported="true" />

        <!-- 消息接收监听器 （用户可自主扩展） -->
   <!--     <receiver
            android:name=".MyMessageReceiver"
            android:exported="false"> &lt;!&ndash; 为保证receiver安全，建议设置不可导出，如需对其他应用开放可通过android：permission进行限制 &ndash;&gt;
            <intent-filter>
                <action android:name="com.alibaba.push2.action.NOTIFICATION_OPENED" />
            </intent-filter>
            <intent-filter>
                <action android:name="com.alibaba.push2.action.NOTIFICATION_REMOVED" />
            </intent-filter>
            <intent-filter>
                <action android:name="com.alibaba.sdk.android.push.RECEIVE" />
            </intent-filter>
        </receiver>-->

    </application>

</manifest>
